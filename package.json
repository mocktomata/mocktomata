{
  "name": "komondor",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "build": "tsc -b",
    "build:watch": "tsc -b -w",
    "test": "yarn jest",
    "verify": "yarn test",
    "watch": "yarn jest --watch"
  },
  "dependencies": {},
  "devDependencies": {
    "@jest-runner/electron": "1.1.1",
    "@types/ramda": "^0.26.5",
    "@unional/devpkg-node": "^1.2.5",
    "lerna": "^3.13.1"
  }
}
