import { createStore } from 'global-store';
import { createDefaultConfig } from './createDefaultConfig';
const store = createStore('@komondor/config', createDefaultConfig());
export { store };
//# sourceMappingURL=store.js.map