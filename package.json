{
  "name": "komondor",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "tsc -b -w",
    "test": "yarn jest",
    "verify": "yarn test",
    "watch": "yarn jest --watch"
  },
  "dependencies": {
    "@jest-runner/electron": "^0.1.0",
    "@types/ramda": "^0.25.45",
    "@unional/devpkg-node": "^1.1.4"
  },
  "devDependencies": {
    "lerna": "^3.8.0"
  }
}
