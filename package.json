{
  "name": "komondor",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "test": "yarn jest",
    "watch": "yarn jest --watch"
  },
  "dependencies": {
    "@jest-runner/electron": "^0.1.0",
    "@types/ramda": "^0.25.45",
    "@unional/devpkg-node": "^1.1.2"
  },
  "devDependencies": {
    "lerna": "^3.4.3"
  }
}
