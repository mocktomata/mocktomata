// import a from 'assertron';
// import isNode from 'is-node';
// import { config, MissingConfigForFeature } from '../config';
// import { createClientIO } from './client';

// if (isNode) {
//   it.skip('No test for remoteIO under Node', () => { return })
// }
// else {
//   it('configuring clientOptions.baseUrl is required', async () => {
//     const err = a.throws(() => createClientIO(), MissingConfigForFeature)
//     a.satisfy(err, {
//       configPath: 'clientOptions.baseUrl'
//     })
//   })
// }

// afterEach(() => config({}))

test.skip('no etst', () => { return })
